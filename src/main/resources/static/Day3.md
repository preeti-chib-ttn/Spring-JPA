# Day 3 - Spring Data JPA

Q1) Create a class `Address` for `Author` with instance variables:
- `streetNumber`
- `location`
- `state`

---

Q2) Create an instance variable of `Address` class inside `Author` class and save it as an **embedded object**.

---

Q3) Introduce a **List of subjects** for the `Author`.

---

Q4) Persist **3 subjects** for each `Author`.

---

Q5) Create an **Entity** `Book` with an instance variable:
- `bookName`

---

Q6) Implement **One-to-One mapping** between `Author` and `Book`.

---

Q7) Implement **One-to-Many Mapping** between `Author` and `Book` in the following ways:
1. **Unidirectional**
2. **Bidirectional**
3. **Without an additional table**
4. **Implement cascade save**

---

Q8) Implement **Many-to-Many Mapping** between `Author` and `Book`.

---

Q9) Which method on the **session object** can be used to remove an object from the **cache**?

---

Q10) What does the `@Transactional` annotation do?  
